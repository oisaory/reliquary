{% extends 'base.html.twig' %}

{% block title %}{{ 'error.not_found.title'|trans({}, 'error') }}{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <img src="{{ asset('/images/relic.png') }}" width="100" height="100" class="mb-4" alt="{{ 'error.logo_alt'|trans({}, 'error') }}">
            <h1 class="display-4 mb-4">{{ 'error.not_found.heading'|trans({}, 'error') }}</h1>
            <div class="card mb-4">
                <div class="card-body">
                    <p class="lead">{{ 'error.not_found.removed'|trans({}, 'error') }}</p>
                    <p>{{ 'error.not_found.check_url'|trans({}, 'error') }}</p>
                </div>
            </div>
            <div class="d-grid gap-3 d-sm-flex justify-content-sm-center">
                <a href="{{ path('app_home') }}" class="btn btn-primary btn-lg px-4 gap-3">
                    <i class="fas fa-home me-2"></i>
                    {{ 'error.return_home'|trans({}, 'error') }}
                </a>
                <a href="{{ path('app_relic_index') }}" class="btn btn-outline-secondary btn-lg px-4">
                    {{ 'error.not_found.view_relics'|trans({}, 'error') }}
                </a>
                <a href="{{ path('app_saint_index') }}" class="btn btn-outline-secondary btn-lg px-4">
                    {{ 'error.not_found.view_saints'|trans({}, 'error') }}
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}